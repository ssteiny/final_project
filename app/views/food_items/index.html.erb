<div class="page-header">
  <br>
  <h1>
    Foods </h1>
    
    <a href="/food_items/new" class="btn btn-lg btn-success">New Food</a><br>
  
  

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th>Actions</th>
      </tr>

      <% @food_items.each do |food_item| %>
      <tr>
        <td><%= food_item.name %></td>
        <td>
          <a href="/food_items/<%= food_item.id %>" class="btn btn-primary">Show</a>
          <a href="/food_items/<%= food_item.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_food_item/<%= food_item.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
    <%= paginate @food_items, theme: 'twitter-bootstrap-4' %>
  </div>
</div>


<div class="row mb-1">
  <div class="col-md-12">
    <%= search_form_for @q, :id => "food_items_filters" do |f| %>
      <p class="lead">Narrow results:</p>

        <div class="form-group">
    <%= f.label :name_cont, "Name contains" %>
    <%= f.text_field :name_cont, :class => "form-control", :placeholder => "Name contains" %>
  </div>

      <script type="text/javascript">
          $(function () {
              $('#created_atgteqPicker').datetimepicker({
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atlteqPicker').datetimepicker({
                  useCurrent: false,
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atgteqPicker').on("dp.change", function (e) {
                  $('#created_atlteqPicker').data("DateTimePicker").minDate(e.date);
              });
              $('#created_atlteqPicker').on("dp.change", function (e) {
                  $('#created_atgteqPicker').data("DateTimePicker").maxDate(e.date);
              });
          });
      </script>

        <div class="form-group">
      <%= f.label :bars_name_cont, "Bar name contains" %>
      <%= f.text_field :bars_name_cont, :class => "form-control", :placeholder => "Bar name contains" %>
    </div>

      <%= f.submit :class => "btn btn-primary btn-block mb-1" %>

      <a href="/food_items" class="btn btn-default btn-block mb-1">Clear filters</a>
    <% end %>
    <a class="btn btn-default btn-block mb-1" data-toggle="collapse" href="#food_items_filters">
      Show Filters
    </a>
  </div>
</div>